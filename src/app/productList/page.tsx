import TabMenu from "@/components/gnb/TabMenu";
import { Button } from "@/components/ui/button";

/**
 * ProductList 컴포넌트.
 *
 * 이 컴포넌트는 TabMenu 컴포넌트를 포함하는 React 프래그먼트를 렌더링합니다.
 */
export default function ProductList() {
  return (
    <>
      <TabMenu />
    </>
  )
}